#include <iostream>
#include <vector>

int main()
{
  std::vector<int> last_jedi;

  // int year;

  // std::cout << "Choose a year:\n";
  // std::cin >> year;

  // if (year < 1000 || year > 10000)
  // {
  //   std::cout << "Year needs to have 4 digits\n";
  // }
  // else if ((year % 4 == 0 && year % 100 == 0 && year % 400 == 0) || year % 4 == 0)
  // {
  //   std::cout << "It's a leap year\n";
  // }
  // else
  // {
  //   std::cout << "It is not a leap year\n";
  // }
}